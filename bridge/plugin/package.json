{
  "name": "@antgrid/plugin",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "mcp": "bun run ../src/index.ts mcp",
    "install-plugin": "bun run setup.ts install",
    "uninstall-plugin": "bun run setup.ts uninstall"
  }
}
