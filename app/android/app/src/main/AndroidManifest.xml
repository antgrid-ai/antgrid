<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:label="antgrid"
        android:name="${applicationName}"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize">
            <!-- resizeableActivity is what makes OEM launchers (MIUI/ColorOS/One UI)
                 offer the "Floating window" / small-window shortcut; the platform
                 default is unreliable across OEM skins, so declare it explicitly.
                 The <layout> bounds keep the small window from opening too cramped
                 for the mobile agent/workspace swipe UI (< ~300dp gets unusable). -->
            <layout
                android:defaultWidth="400dp"
                android:defaultHeight="640dp"
                android:minWidth="300dp"
                android:minHeight="480dp"
                android:gravity="center" />
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- The embedding's own deep-link handling defaults to ON and
                 forwards every VIEW intent below to Flutter's navigation
                 channel, where a home-only MaterialApp can only crash (see
                 PlatformRouteGuard in lib/navigation/). The app_links plugin
                 reads the intent through its own listener, so turning this off
                 costs nothing and is what the plugin expects. -->
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Hosts stay enumerated: dropping the restriction would hand
                     this activity every antgrid:// URL, including hosts nothing
                     handles. Keep in lockstep with handleLink in lib/main.dart,
                     which dispatches `nav` to the NavLocation codec and `auth`
                     to OAuth token redemption.
                     Android merges <data> attributes across a filter as a cross
                     product, so a second scheme added here would also inherit
                     every host below. One scheme per filter. -->
                <data android:scheme="antgrid" android:host="auth" />
                <data android:scheme="antgrid" android:host="nav" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Names the Dart entrypoint our push fork runs in the headless engine
             on a terminated-state push. Without it push runs main(), which would
             drag the whole app bootstrap (stores, provisioning, runApp) into a
             30s broadcast receiver. Keep in lockstep with pushBackgroundMain in
             lib/main.dart. -->
        <meta-data
            android:name="uk.orth.push.background_entrypoint"
            android:value="pushBackgroundMain" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
