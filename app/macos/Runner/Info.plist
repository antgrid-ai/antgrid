<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string></string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSHumanReadableCopyright</key>
	<string>$(PRODUCT_COPYRIGHT)</string>
	<key>NSMainNibFile</key>
	<string>MainMenu</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<!-- Sparkle (auto_updater plugin). Automatic checks stay OFF: detection is
	     the app's own poll of the same appcast Sparkle installs from
	     (UpdateGate -> drawer UpdateRow), and
	     Sparkle only runs when the user clicks Update. Without this key Sparkle
	     would show its own "check automatically?" permission prompt at launch.
	     No SUPublicEDKey: updates verify via the Developer ID code signature
	     (old and new app signed by the same team). Adding a key here is a
	     one-way door — every later enclosure must then carry a valid
	     edSignature, and the key can be rotated only by a coordinated
	     release, never silently. The "Generate Sparkle appcast" step in
	     .github/workflows/build-desktop.yml must already be signing
	     appcasts before this key ships; it owns the matching gate. -->
	<key>SUEnableAutomaticChecks</key>
	<false/>
	<key>SUAutomaticallyUpdate</key>
	<false/>
	<!-- app_links owns every antgrid:// URL (handleLink in lib/main.dart).
	     Letting the embedder route them instead pushes the URI at a home-only
	     MaterialApp, which can only crash — see PlatformRouteGuard in
	     lib/navigation/. -->
	<key>FlutterDeepLinkingEnabled</key>
	<false/>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>com.antgrid.auth</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>antgrid</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
