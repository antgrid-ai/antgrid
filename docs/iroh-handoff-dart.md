# Dart PeerLink handoff

Implemented Wave 1 in `packages/antgrid_relay_client/`.

Changed: `lib/src/peer_link.dart` (new interface), public barrel, `relay_service.dart` (WebSocket adapter and explicit admission outcomes), `machine_session.dart` (payload lifecycle and authorization fence), `connection_handshake.dart` (both drivers), package `CLAUDE.md`, test fakes, and `test/peer_link_test.dart`.

Contracts: existing `relay:` constructor names remain, now accepting `PeerLink`. Central RelayService state/presence are separate from payload lifecycle/path. WebSocket adapts offline/online into peer restart, preserving rekey behavior. Native transports must not use central presence to emit restart. `isDispatchAllowed` must be cleared synchronously by lease wrappers before lifecycle delivery. Session decrypt dispatch and send dequeue recheck this fence. Admission futures mean local transport acceptance only; failed sends do not charge credits. Existing void `sendMessage` remains for source compatibility, while fake subclasses overriding it must also override `sendFrame`.

Checks: `git diff --check -- packages/antgrid_relay_client` passed. No Dart/Flutter executable was started (parent owns serial format, analysis and test gate). Tests authored for closed/oversized send outcomes and a non-relay link with path changes that leave E2E intact; close erases session keys. Existing package fake transports updated.

Not claimed: native transport, lease implementation, platform qualification or E2E staging results. The WebSocket adapter continues relying on the existing session scheduler/credits for aggregate socket bounds; it adds no independent queue.

## Wave 2/3 transport package and app integration

New ELv2 package `packages/antgrid_peer_transport` contains authorization snapshot parsing and decimal generations, domain-separated challenge bytes, dual-key endpoint enrollment through injected authenticated HTTP, monotonic request-start-bounded authorization leases, a lease wrapper shared by WS/native, bounded native record transport, one enrollment endpoint owner, and generation-fenced transport selection. Public upstream pins are iroh_quic 1.0.3 / flutter_rust_bridge 2.12.0; cryptography is explicit. Parent owns generated lockfiles and serial tool gates.

App ownership added: `connection/peer_runtime.dart`, `providers/peer_runtime.dart`, `connection/relay_mechanisms.dart`, `providers/relay_connection.dart`, `providers/agent_transport.dart`, `providers/control_plane.dart`, `connection/connection_supervisor.dart`, `connection/supervisor_state.dart`, related tests and scoped CLAUDE. App runtime shares one endpoint per account enrollment, uses the separate stored endpointSecret, authenticates endpoint API requests by device bearer, registers against authoritative registrationGeneration, serializes lease refresh, and retains refreshed leases across new sibling-machine dials. Resume fences leases before refresh. Refcounts stop periodic refresh when no remote connections remain. Approved-relay-map changes fence native dispatch and rebuild the shared endpoint on the next connection attempt.

Central connection startup runs alongside native selection; the WebSocket fallback awaits central admission. Native establishment can succeed with fresh HTTP authorization during a central outage. Healthy native payload connections stay Connected during central presence/path changes. Central restoration is single-flight with supervisor-owned backoff and does not monopolize payload recovery. Terminal peer rejection has a sticky retry-only supervisor state. The control-plane provider derives presence from effective supervisor status so a central-only outage does not erase live project adverts. Payload replacement emits the existing session replacement/hydration path; pending commands are not replayed.

Additional security review fixes: native/WS selection captures app-key, endpoint-registration and relay-policy state before asynchronous native work and rechecks afterward and at native dequeue; unknown upstream native connection/read/write/remote-close errors are terminal because pinned bindings expose no security-safe typed close reason; handshake crypto awaits now fence cancellation/authorization and late derived keys are scrubbed rather than transferred; policy-generation control events are separate from payload lifecycle. Native physical controller disposal is deferred until active synchronous stream callbacks return, while the authorization gate closes synchronously.

Tests authored: pure Dart authorization deadlines/serialization/highwater, enrollment dual signatures and cross-device rejection, selection cancellation/late completion/no fallback on denial; app supervisor sticky peer rejection and independent central single-flight maintenance; app payload lifecycle integration (central outage/reconnect/path changes preserve session, late selection after release closes). No Dart/Flutter process was started by this agent. Parent reported the earlier Wave1 format/analyze succeeded and initially discovered teardown/test-fixture regressions, both fixed; parent owns current full reruns. Parent's actual native DLL smoke now passes echo, oversize records, extra streams and revoked admission after the reentrant-controller teardown fix.

Unqualified/release blockers: real staging backend/relay native admission and revocation races; real Flutter native packaging/hardware; full workload performance/network matrix; precise direct-vs-relay path metrics (binding currently emits address strings; adapter reports unknown rather than guesses); upstream typed native close causes; guaranteed erasure of private key copies retained by upstream SecretKey (no public zeroize/dispose method). App-owned seed and E2E session buffers are scrubbed and no global RustLib.dispose is used for peer cleanup. The committed code does not establish that these release blockers passed.
## Final local verification note

At the parent's explicit request, ran only the SDK executable formatter (bypassing Flutter/Dart batch startup locks): `C:/Users/Admin/Downloads/flutter/bin/cache/dart-sdk/bin/dart.exe format` over the owned app connection/provider/tests and new package/lib/tests plus handshake files. It completed successfully. No analyzer/test/native process was started by this agent. Parent owns all final gate results. The authorization catch-path generation fence and regression test were added before that format pass.

## Dart terminal evaluation follow-up

The parent real Dart terminal gate exposed an obsolete evaluator expectation: the production handshaker advertises terminalFramesV1, so the bridge intentionally suppresses legacy terminal:output, while the Dart scenario awaited that retired stream without subscribing. Added `DartAppClient.waitForTerminalFrameContaining` with versioned subscribe, run/attachment scoping, frame-schema validation, ACKs and unsubscribe; updated both Dart terminal scenario files to assert frame ANSI markers. No transport behavior or compatibility capability was weakened. `git diff --check` passed; parent owns real eval rerun.

## Protected enrollment migration follow-up

Cached primary and desktop-controller records now lazily persist a separate random endpoint seed through their existing serialized provisioning operations, preserving every identity/OAuth field. Storage failure throws without returning an ephemeral endpoint identity. Connection identity resolution invokes that migration; a missing seed or device-token minter at peerRuntimeProvider is terminal rather than silently selecting an unleased WebSocket. Tests cover both slots, concurrent migration, persistence failure, and missing runtime credentials. The app contains no current agent:enable-relay sender (searched app/lib); bootstrap forwarding remains parent-owned and bridge accepts the optional fields for external callers.

Adapted existing app fake RelayService subclasses to implement asynchronous sendFrame with real fake-state admission checks; identity/coordinate provider fixtures explicitly override the HTTP/native runtime so they remain scoped to their original behavior. The parent full suite compiled while these files were changing and reported migration failures consistent with old cached-return source; rerun is required. No Dart process was started for these edits. git diff --check passed.

## Desktop bridge resume follow-up

AppShell now notifies the local host on the actual onResume callback (not the paired onRestart callback). HostController reads existing discovery without ensureHost, ping, process creation or replacement. HostControlClient posts an empty body to /peer-resume using the existing owner bearer token and accepts only HTTP 202 with ok:true; timeout is bounded and errors contain no credentials or response payload. The bridge contract acknowledges synchronous old-lease fencing and starts fresh authorization asynchronously. Auth/path/body/status/timeout tests and controller no-spawn tests were added. Scoped app CLAUDE records the cross-component route contract. No Dart tools were run; parent owns format and test gates. Targeted git diff --check passed.

## Native library selection follow-up

NativeEndpointOwner.create accepts an injected asynchronous native initializer. The app uses only public iroh_flutter Iroh.init with an absolute bundled sibling DLL on Windows and executable-directory/lib SO on Linux; Android passes the packaged SO name explicitly. This bypasses upstream cwd Rust output and user-cache lookup. CLI callers retain the upstream default signed-cache setup. Apple still uses the public default because upstream exposes no process-symbol-only option; its disk-first lookup is an unresolved Apple native release blocker, and no private FRB initializer or guessed framework path was added. A unit test verifies injected initialization failure propagates without continuing to key access or default initialization. Native diagnostics now forward to the relay's current tap dynamically, so arming netwatch after selection works. Parent owns formatting, tests and actual bundle verification; targeted diff-check passed.

### Apple public API review

Re-read pinned cached iroh_quic lib/src/runtime.dart and lib/src/ffi/loader.dart, iroh_flutter's public re-export and both Apple podspecs, plus app/ios/Podfile and app/macos/Podfile. Public Iroh.init exposes only an optional libraryPath. Both app targets use_frameworks!, and upstream force-loads libirohdart_ffi.a into the iroh_flutter dynamic framework so process symbol lookup works. It does not bundle a separate libirohdart_ffi.dylib at a demonstrated path. No generated CocoaPods products or Apple build are available here to verify an explicit framework image path, so no guessed path was introduced. The default loader itself does not download anything: it first checks cwd Rust outputs and the CLI setup cache, then falls back to process symbols. Thus the unresolved requirement is deterministic bundled Apple image selection through a supported API, plus real signed Apple package verification. Upstream would need a public process-only option, or an Apple build must establish the exact bundled framework path before wiring libraryPath.

The review also found upstream initialization uses only a completed boolean around asynchronous FRB init. App initialization now shares one process-wide future across account runtime replacement, preventing two enrollment owners from entering that window concurrently; failed initialization clears the future for a later authorized retry. No process-global disposal is introduced. Substantive remaining native gaps are upstream typed terminal close classification (conservative terminal handling remains), direct-vs-relay path metrics, upstream-owned SecretKey zeroization, and external Apple/mobile/network qualification. No Dart tool was run during this review.

## Selection lifecycle telemetry

Added optional diagnostics to PeerLinkSelector and a fail-open lifecycle event helper reused by native dial and app PeerRuntime. Shape follows the existing netwatch map/detail adapter with additive op:lifecycle, dir:event, kind:lifecycle, transport:iroh|relay. Stages are fixed: peer:connection-request, peer:authorization-ready, peer:selection-start, peer:iroh-established, peer:selection-timeout, peer:fallback, peer:transport-selected, peer:selection-rejected. Detail contains monotonic elapsedMs and selection generation where available; fallback reasons are a fixed allowlist, unknown codes become a generic classification. No endpoint, address, payload, key, bytes or frameId is emitted. Dynamic relay tap forwarding allows capture arming after runtime construction. Parent owns matching bridge netwatch type/ingest/CLI additions.

Timings have explicit local scopes: authorization-ready measures from runtime select request, selector stages from selection start (after authoritative preparation), and iroh-established measures authenticated connection plus one bidirectional stream readiness from native dial start. That stage does not claim E2E or terminal readiness. Existing MachineSession/RelayService capture covers handshake/control/sealed frames and request annotations, not dedicated E2E-established/project-binding/usable-terminal timing. Those stages, separate central authentication/discovery timing, and CPU/memory/path-byte classification remain absent from this bounded task. Tests cover timeout/fallback event ordering, absence of wire-byte metadata, terminal no-fallback classification, unknown-code redaction and observer exceptions not changing selection. No Dart command was run; diff-check passed and parent owns gates.

## Bridge payload telemetry correction

Scoped changes: bridge/src/peer-session-owner.ts, bridge/src/peer/iroh-relay-client.ts, bridge/tests/iroh-relay-client.test.ts. Shared session diagnostics now ask a protected payloadTransport(peerId) hook; the native subclass classifies only its own peer entries as iroh. Queue drops retain explicit transport through native teardown after the peer map entry is removed. No mutable current-peer/current-transport global was added. Generic broadcast drops with no recipient retain the default relay classification and represent no successful bytes. Shared diagnostic recording catches observer exceptions.

Both native send paths emit successful frame metadata only after PeerRecords reports sent. The bytes field retains the existing encrypted/handshake payload-length convention; detail separately reports routeBytes, recordBytes (route plus four-byte length prefix), and lengthPrefixBytes. No plaintext/body capture or direct-versus-relay network-path byte claim is added. Native lifecycle events record native acceptance (from acceptPeer entry through authorization/first stream), E2E establishment (from native acceptance), and native project-local admission (from attachStream). Local admission does not claim the remote app bound a project or rendered a usable terminal.

Verification: initial focused run had 25 pass/1 fail because the throwing-observer fixture also replaced unrelated central sendJson instrumentation; fixture was scoped to the native observer boundary. Then bun run --filter antgrid-bridge test tests/iroh-relay-client.test.ts tests/netwatch.test.ts tests/send-scheduler.test.ts passed 43/0. After adding assertions for the scheduled native send path, bun run --filter antgrid-bridge test tests/iroh-relay-client.test.ts passed 10/0. Targeted git diff --check passed. Bridge typecheck was started separately and its final result is recorded below when complete. No Dart commands were run.

Bridge typecheck completed: bun run --filter antgrid-bridge typecheck exited 0.

## Modern terminal scenario repair

Changed evals/scenarios/terminal/terminal.test.ts and multi-terminal.test.ts; added scoped frame-output.ts with parent-coordinated ownership. The shared helper validates existing AbMessageSchema messages, negotiates the current terminal protocol, correlates subscription request/terminal/run/attachment, ACKs consumed frames, bounds the overall wait and unsubscribes in finally. Single-terminal output and input assertions inspect actual rendered ANSI output; the two-terminal scenario verifies each terminal ID and excludes the other terminal's marker. No capability opt-out or legacy fallback was introduced. Initial execution failed because a direct zod import is not an eval dependency; switching to the existing bridge schema removed that dependency without lockfile edits. Final bun run --filter antgrid-evals test:evals:terminal passed 4/0 (14 assertions, 5.39 seconds). No Dart command was run.

## Real backend to Rust relay gate

Changed evals/support/iroh-authorization.ts only to accept optional typed environment overrides. Added evals/tests/gate-iroh-relay-authorization.test.ts and iroh-relay/examples/real_backend_gate.rs; parent added the workspace script. The example uses production relay_connection, State, Gate and authorization maintenance against the actual backend HTTP route, with upstream iroh-relay 1.2.0 clients. It generates a localhost certificate and explicitly trusts that certificate; TLS verification remains enabled. Only the test socket is ephemeral: the validated production config still requires port 443. The TypeScript harness provisions actual device OAuth credentials and dual-signed endpoint registrations for test seeds 5/6 in a real migrated Postgres database, and turns machine remote access on through its authenticated heartbeat.

Gate assertions: two registered endpoints exchange packets in both directions; an unregistered endpoint is denied; actual DELETE /account/devices/:id revocation closes both account relay connections within 60 seconds and a fresh connection using the revoked endpoint is refused. PEER_POLICY_TARGETS is empty to exercise the refresh/lease outage backstop independently of push delivery. The final backend authorization request also returns 401 for the revoked credential. Backend service HMAC secret is random and passed over stdin, not command arguments or logs. No fake admission backend or TLS-verification override is used.

Prerequisites: working Postgres admin URL as for the existing backend gates, generated Prisma client/migrations, installed workspace dependencies, and the locked Rust toolchain. Build with cargo build --locked -j 2 --manifest-path iroh-relay/Cargo.toml --example real_backend_gate; set ANTGRID_IROH_RELAY_GATE only if using a binary outside the default target/debug/examples location. This agent used workspace CARGO_HOME=.tmp/iroh-rust-toolchain/cargo, RUSTUP_HOME=.tmp/iroh-rust-toolchain/rustup and stable Rust 1.98.1. Build passed in 13.33 seconds; scoped rustfmt and git diff --check passed. bun run --filter antgrid-evals test:evals:iroh-relay-authorization passed 1/0 with 11 assertions (test 23.54 seconds, runner 24.33 seconds). No Dart command was run.

Qualification boundary: this is actual service admission and relay-packet traffic over trusted TLS with real backend revocation. It does not establish app/bridge native QUIC and application E2E over that same self-hosted relay, production deployment/TLS provisioning, physical platform packaging, or network/performance qualification. Those remain separate gates.
