name: antgrid_eval_client
description: CLI wrapper around antgrid_relay_client for eval testing.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  antgrid_relay_client:
    path: ../antgrid_relay_client
  antgrid_peer_transport:
    path: ../antgrid_peer_transport
  iroh_quic: 1.0.3
  uuid: ^4.5.1
